# MB Partners 微調整 R3 — 統合レポート（2026-06-16）

自走・無確認で実装。build+検証→ vercel --prod（最終 `96gfj5t50`）。host/role分離・既存deal frozen・既存payout/snapshot 不変。

| # | 項目 | 対応 |
|---|---|---|
| ① | APPヘッダー右上を「アイコンのみ」 | `app/app/layout.tsx`：プロフィールリンクから名前 `<b>` を削除しアバター(円)のみ。タップでマイページ＝従来どおり。 |
| ② | コンソール お客様名に敬称（全箇所） | `lib/customer` を ダッシュボード(最近の動き/商談スケジュール)・案件ボード(カード/ドロワー)・パートナー詳細の案件一覧・グローバル検索 に適用（個人「氏名 様」/法人「会社名 御中＋担当者 様」）。 |
| ③ | ダッシュボードに担当パートナー名 | 「最近の動き」「商談スケジュール」にお客様名(様)＋担当パートナー名を併記。 |
| ④ | 案件ボードのアイコン刷新 | `ServiceIcon`→`ServiceAvatar`（ロゴ→フォールバックアイコン）に統一。 |
| ⑤ | 案件ボードカードの情報整理 | サービス名テキストを削除し、代わりに紹介者（担当パートナー：ミニアバター＋氏名）を表示。誰の・どのお客様・進捗(列)が一目で分かる構成を維持。 |
| ⑥ | 「サービス」→「サービスマスタ」 | コンソール ConsoleNav ラベル＋サービス管理画面見出しを変更。APP側のユーザー向け表記（サービス選択 等）は不変。 |

## 技術メモ（RLS）
owner 認証では nested 埋め込み `partners.profiles` が RLS で null（code は取得可・名前/色は不可）になり、②③⑤の担当名が出ない問題を確認。所有・権限を確認済みの **コンソール案件取得（`/api/console/deals` GET）・ダッシュボード（getAllDeals/deal_events）を service role 読取に変更**して解決（host/role分離は不変）。

## 検証（本番ビルド）
- ① ヘッダー右上＝アバターのみ（名前非表示）。
- ②③ ダッシュボード：お客様名 様/御中＋「担当: 〇〇」表示。商談スケジュールも様付き＋担当。
- ④⑤ 案件ボード：ロゴ表示・お客様 様付き・サービス名削除→紹介者（担当）表示。
- ⑥ nav/見出し＝サービスマスタ。
- pageエラー 0。

## スクショ
`docs/reports/review_screens/r3/`：app_header.png（①）／console_dashboard.png（②③⑥）／console_board.png（④⑤）。
