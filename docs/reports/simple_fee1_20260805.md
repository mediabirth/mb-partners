# SIMPLE-FEE-1 — 手数料の単純化 統合レポート

## 実行情報

- 実行日: 2026-08-05 JST
- 実行モデル: GPT-5.6 Sol
- 仕様正典: `docs/design/lineage-rate-design.md` §11 v4（勝彦承認記録 2026-08-05）
- 起点SHA: `9c285878cb846f80597333d0f89df83cdafbe8c0`
- rollback tag: `rollback-simple-fee1-20260805-baseline`
- deploy tag: `deploy-simple-fee1-20260805`
- 完了SHA・deployment ID・本番stamp: deploy tagの実測値をチャット統合レポートへ記載する。

## 着手前の停止条件

DB書込前に次を再実測した。

| 停止条件 | 実測 | 判定 |
|---|---:|---|
| `supplier_charges` | 0件 | 続行可 |
| `deals.fee_snapshot is not null` | 0件 | 続行可 |

凍結済みデータが存在しない承認済みの窓であることを確認してから着手した。実ユーザー、実案件、`cc-monitor` は操作していない。

## 実装内容

| 発注 | 実装 |
|---|---|
| 月額機構の撤去 | `OMNIS_MONTHLY_FEE`、`RateCard.monthly_fee`、`FeeSnapshot.rate_kind='omnis_monthly'`、self-serviceの月額分岐、`ChargeRow.kind='omnis_monthly'`、月次クローズの案件外固定行を削除。 |
| API | レートカードGET/POSTの返却列を明示し、残置DB列を製品APIへ出さない。新規カードは50%／5%／10%を固定し、意味差を `fee_model` に限定。supplier charge APIの種類コメントも3種へ更新。 |
| UI | supplier money、console支払、supplier一覧・詳細、レートカード付替え確認から月額入力・表示・説明を撤去。カード説明を「折半（オムニス等の個別契約）／標準（パススルー＋受注額5%）」へ簡素化。 |
| ⓘ | `console-guides` と `supplier-guides` を同一バッチで現仕様へ追随。 |
| 検証資産 | `scripts/simple-fee1.e2e.mts` と `pnpm test:simple-fee1` を追加。旧 `supplier-mgmt.e2e.mts` の月額前提を決済5%へ更新。 |
| 文書 | 現行設計を参照する `partner-reward-override-design.md` の旧月額説明を現仕様へ追随。§11より前の設計・DDL・撤去台帳・デモ台帳は歴史証跡として変更しない。 |

## DBバックアップと再定義

実行前COPY:

- ディレクトリ: `/private/tmp/mb-simple-fee1-backup-20260805-2118`（mode 700）
- ファイル: `rate_cards_omnis-founding-v1_before.csv`（mode 600、COPY 1行）
- SHA-256: `6a5f8ca2e99e899176248b8cbbb94867d88384e108134b6d167a5c54b696beca`

実行SQL全文:

```sql
begin;
do $$
begin
  if (select count(*) from supplier_charges) <> 0 then
    raise exception 'SIMPLE-FEE-1 aborted: supplier_charges is not empty';
  end if;
  if (select count(*) from deals where fee_snapshot is not null) <> 0 then
    raise exception 'SIMPLE-FEE-1 aborted: fee_snapshot deals exist';
  end if;
end $$;
update rate_cards
set monthly_fee = null,
    payment_fee_rate = 0.05
where id = 'omnis-founding-v1'
returning id, fee_model, half_commission_rate, payment_fee_rate,
          monthly_fee, override_rate, revenue_fee_rate, deprecated;
commit;
```

結果:

```text
omnis-founding-v1|half_commission|0.5|0.05|NULL|0.10|NULL|false
UPDATE 1
```

復元手順（必要時のみ。実行前に現状COPYを追加取得する）:

```sql
begin;
create temporary table simple_fee1_restore
(like rate_cards including defaults) on commit drop;
-- psql側で以下を実行
-- \copy simple_fee1_restore from '/private/tmp/mb-simple-fee1-backup-20260805-2118/rate_cards_omnis-founding-v1_before.csv' csv header
update rate_cards r
set monthly_fee = b.monthly_fee,
    payment_fee_rate = b.payment_fee_rate
from simple_fee1_restore b
where r.id = b.id and r.id = 'omnis-founding-v1';
commit;
```

## 不変領域の機械証明

- `supplierChargeBase` から `validateSupplierReward` 末尾までのSHA-256 before/after:
  `f62c17f77709d0257c8d2008ceb3725f73e6b8952b163489ffdd683810c6958a`
- `freezeFeeSnapshot` 関数本体SHA-256 before/after:
  `560a8c5ed2d3f0086648bdefbee22f25e27c2b00147f6b346889118716b44692`
- `lib/frontier.ts` / `lib/frontier-payout.ts`: `git diff --quiet` 一致。
- 折半行、決済5%行、パススルー行の算式は無変更。削除差分は月額固定ブロックのみ。
- 法人override、2段凍結、`supplierChargeBase`、`validateSupplierReward` の逆ザヤ規則は無変更。

## grep全数監査

- 製品・実行コードの識別子 `omnis_monthly|monthly_fee|OMNIS_MONTHLY`:
  `app lib components scripts` **0件**。
- 製品コードの表示語「月額」: `app lib components` **0件**。
- scriptsの「月額」: **4件**。すべて「含まれないこと」を検査する負のassertion（専用3、旧supplier E2E 1）。
- docs内の残存はすべて対象外の歴史／正典文脈:
  - `lineage-rate-design.md`: 旧§の歴史記述＋§11の撤去命令（20行）
  - `lineage-rate-review.md`: v1レビュー史（3行）
  - `featureH_lineage_p0a_ddl.sql` / `featureI_supplier_mgmt_ddl.sql` / `featureI2_standard_v2_ddl.sql`: 実行済みDDL台帳（3/4/2行）
  - `demo_seed_manifest_20260712.json`: 撤去済みデモ台帳（7行）
  - `removal-ledger-20260724.md`: 撤去履歴（1行）
  - `RESUME.md`: SIMPLE-FEE-1の承認済み裁定（1行）
- DB列 `rate_cards.monthly_fee` と既存CHECK制約は、発注どおりDDL互換のため残置。製品参照は0。

## 専用E2E・実画面

`pnpm test:simple-fee1`: **6/6 green**。

1. 他系統→折半カードメニュー: `half_commission / 0.5`
2. 同系統→自社メニュー: foundingカードでも `payment_fee_5 / 0.05`
3. 月次クローズpreview: 全行 `deal_id != null`、案件外固定行0
4. 折半額: `¥200,000 × 50% = ¥100,000`
5. 決済手数料: `¥10,000 × 5% = ¥500`
6. throwaway残置0

実ブラウザbefore/afterは `docs/reports/simple_fee1_screens/` に6枚保存。

| 面 | before | after |
|---|---|---|
| supplier money | サービス利用料¥100,000＋月額利用料¥50,000＝¥150,000 | サービス利用料¥100,000＋決済手数料¥500＝¥100,500 |
| console suppliers | foundingカードに旧月額を含む請求見込み | 月額行なし、案件ベースの¥100,500 |
| レートカード詳細 | 「折半50%／月額¥50,000／override10%」 | 「折半（オムニス等の個別契約）」と「標準（パススルー＋受注額5%）」 |

- supplier money: 375×667で `innerWidth=375 / document.scrollWidth=375 / body.scrollWidth=375`
- 画面本文の「月額」: false
- page errors: `[]`

## 品質ゲート7項目

| ゲート | 証跡 |
|---|---|
| 静音 | カード説明を2行文法へ削減。常設説明を追加していない。 |
| 平易語彙 | 「折半」「標準」「決済手数料」に限定し、内部kindはUI非表示。 |
| ナビ文法 | ナビ・BrandMark非接触。 |
| ⓘ | console/supplier guideを現仕様へ同時更新。 |
| ペルソナの真実 | supplier本人面で自社案件の折半・決済5%だけを表示。案件外固定費を表示しない。 |
| モバイル | 375×667水平溢れ0、page errors 0。モーダル変更なし。 |
| 性能 | 最終全走のperf 3面: console `10/346/39ms`、APP `12/20/39ms`、vendor `10/325/39ms`。全予算内。 |

## 恒久ゲート

- `pnpm test:verify`: **PERMANENT VERIFY GREEN**
- build 0 / typecheck 0
- canon: status-effects 63、coop-task 10、narrative、reward-format、synapse全green
- integrity: 17/17（3面307、webhook 401、公開面375px、page errors `[]`）
- session: 32/32（運営者case[7]込み）
- password-reset: 39/39
- hardening: 43/43
- perf: 3/3 green
- resume reload: 2/2
- resume performance: 21/21

## money・残置・送信

| 証跡 | before | after |
|---|---|---|
| menu_rewards | `4f1b52d0a027a282e188be76e9372193` | `4f1b52d0a027a282e188be76e9372193` |
| deals reward | `(empty)` | `(empty)` |
| fee | `(empty)` | `(empty)` |
| override | `(empty)` | `(empty)` |
| MB seed補助 | 38行 / 340,320 | 38行 / 340,320 |
| 凍結済み | charges 0 / fee_snapshot deals 0 | charges 0 / fee_snapshot deals 0 |

- `CCSF1` relational fixture: 0
- `cc-sf1-*` auth.users: 0
- `cc-sf1-*` mail_log: 0
- 恒久E2Eは `CC_MAIL_SUPPRESS=1`。専用E2Eはメール／Slack入口を呼ばず、実送信0件。
- 意図したDB変更は `rate_cards.id='omnis-founding-v1'` の2列だけ。money 4ハッシュ対象は全桁一致。

## 開示

- 最初のtypecheck時、直前の開発サーバがファイル上限に達して生成途中の `.next/types/validator.ts` を残し、生成物由来のfalse redが1回出た。開発サーバ停止後、当該 `.next` を `/private/tmp/mb-simple-fee1-stale-next-20260805-2120` へ退避し、clean build/typecheckを再実行してgreenを確認。製品ソースの型エラーではない。
- 最終差分後の `test:verify` 2回目で、resume-perf 21件中1件だけconsole human/idle35が566msとなった（他20件green、直前全走21/21）。閾値を変更せず同テストを再走して21/21、続く `test:verify` 全走も21/21・PERMANENT VERIFY GREENで確定した。単発外れ値として開示し、除外はしていない。
- 実ブラウザfixture撤去後、古いrefresh tokenを持つローカル画面が `refresh_token_not_found` を出した。fixture削除後の期待挙動であり、検証中の画面ログは撤去前に `[]` を確認済み。
- 既存未コミット `docs/reports/screens_integrity/*.png` 7点はユーザー所有としてstage/commitしない。
