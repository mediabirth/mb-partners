========================================================
MB Partners — UI/UX 美化バッチB1（デザイン言語＋ダッシュボード）ダンプ
日時: 2026-06-14
正本: 要件定義書 (Notion 37c1d8f5...) ＋ デザイン指針
種別: 見た目と動きのみ。データ・数値・ロジック不変。
ビルド: npx next build → ✅ エラーなし
検証: ローカル認証スクショ(通常/reduced-motion) ＋ 全コンソール8ページ 200/エラーなし
========================================================

■ 変更ファイル
1. app/globals.css                 (⑦) — デザイン言語ユーティリティ追加(共通土台)
2. components/CountUp.tsx           (新規) — 軽量カウントアップ(rAF・reduced-motion尊重)
3. app/console/page.tsx            (②) — ダッシュボード美化
4. app/console/ChannelChart.tsx    (②) — card-hover＋棒グラフ grow-in

========================================================
■ ⑦ デザイン言語（globals.css・全ページ共通の土台／追加のみ・既存classは不変）
========================================================
・.lift                — 行/リンクの微リフト(hoverでtranslateY -1px＋薄青背景)
・.chip / .chip-referral / .chip-cooperation / .chip-direct
                        — システムカラーchip(紹介=青/協力=濃色/直販=中立)
・.feed-dot            — フィード用カラードット
・.shine + @keyframes shineSweep  — ヒーローの一回限りグロス掃引(skewX・left移動)
・.bar-grow + @keyframes barGrow  — 縦棒の下から伸びる grow-in(scaleY)
・.flash-highlight + @keyframes flashHighlight — ステータス変更ハイライト(後続バッチ用土台)
・.celebrate-pop + @keyframes celebratePop     — "嬉しい瞬間"の控えめpop
※すべて transform/opacity 中心の軽量アニメ。既存の
  @media (prefers-reduced-motion: reduce) 全停止ルールで自動的に無効化。

========================================================
■ CountUp.tsx（軽量・速度維持・reduced-motion対応）
========================================================
・requestAnimationFrame + easeOutCubic で 0→値 をアニメ。ライブラリ不使用。
・prefers-reduced-motion: reduce 時は最終値を即時表示(アニメ無し)。
・format は文字列フラグ('number'|'yen') — サーバー→クライアントへ関数を渡さない設計
  (Next の "Functions cannot be passed to Client Components" を回避)。

========================================================
■ ② ダッシュボード "ワクワク" 化（データ/数値/ロジック不変）
========================================================
[今月のハイライト ヒーロー(高揚要素・新規)]
  - 青グラデ＋一回限り shine 掃引＋スパークルSVG＋card-hover。
  - コピーは既存データから生成: 「今月は N件成約・¥X 確定 🎉」/0件時は前向きコピー。
  - 補足行: 稼働パートナーN名・進行中N件。新規クエリ無し(既存値の再利用)。

[KPI 4枚]
  - 各カードに色付きアイコンバッジ(成約=青✓/報酬=緑¥/パートナー=青users/要対応=赤bell)。
  - 数値は <CountUp> でカウントアップ。要対応>0は赤＋赤バッジ。
  - 旧:成約カードのみ青塗り → 全カード白＋アイコン色アクセントに統一(ヒーローが唯一の主役)。

[最近の動き → 活気あるフィード]
  - deal_events のクエリに deals.channel を表示用に追加(数値・ロジックは不変)。
  - 各行: チャネル色ドット＋顧客名＋紹介/協力/直販 chip＋本文。.stagger で出現、.lift で hover。

[チャネル別成約 / 商談スケジュール]
  - ChannelChart: コンテナ card-hover＋月次縦棒 bar-grow(列ごとに60msディレイ)。
  - 商談: コンテナ card-hover、行 .lift、TODAY バッジに pulse。

[余白(呼吸感)]
  - コンテンツ padding 28→30/32/44、グリッド gap 12/14→14/18、カード内 padding 増。

========================================================
■ 速度・データ
========================================================
・サーバー処理の追加なし(新規ラウンドトリップ無し、deal_events に列1つ追加のみ)。
  → 主要ページTTFB据え置き。重いJSアニメ無し(CSS transform/opacity＋rAFカウントのみ)。
・データ/数値/ロジック完全不変(表示のみ)。
・reduced-motion: 全演出停止・最終値即時表示をスクショで確認。
・全コンソール8ページ 200・サーバーエラー無しを確認。
